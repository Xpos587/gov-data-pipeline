name: default

channels:
  - conda-forge

dependencies:
  - python=3.12
  - pip:
      - aiohttp
      - pydantic
      - pydantic_settings
      - polars
      - fastexcel
      - xlsxwriter
      - openpyxl
      - python-docx
      - pillow
      - openai
